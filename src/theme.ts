import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    black: "#2D3748",
    white: "#fafafa",
  },
  h1: {
    color: "black",
    fontSize: "34px",
  },
});

export default theme;
